// import { expect } from 'chai';
// import { ethers } from 'hardhat';

describe('SolarBear', function () {
  // TODO: Rewrite tests
  xit('Should have the correct uri passed in from the constructor', async function () {
    // const SolarBear = await ethers.getContractFactory('SolarBear');
    // const solarBear = await SolarBear.deploy('https://token-cdn-domain/{id}.json');
    // await solarBear.deployed();
    // expect(await solarBear.uri('0')).to.equal('https://token-cdn-domain/{id}.json');
  });
});
